# Minimal dependencies for Lambda deployment
fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
python-dotenv==1.0.0
pydantic==2.5.0
openai==1.3.7
boto3==1.29.7
httpx==0.27.0

# Image processing - lightweight only
Pillow==10.1.0

# REMOVED heavy dependencies:
# - opencv-python-headless (45MB+) - not critical for your use case
# - numpy (30MB+) - only needed if you use opencv
# - python-jose, passlib, bcrypt - auth handled by separate auth service
# - fastapi-mail, email-validator - not used in train service
# - python-multipart - not needed